import React from 'react';
import { Outlet } from 'react-router-dom';

const ForecastOpt = () => {
    return (
        <div>
            forecastOpt
            <Outlet/>
        </div>
    );
};

export default ForecastOpt;